// App.tsx
import './App.css';
import Header from './components/Header';

function App() {

// const song = songStore((state:any) => state.song); 
//  const nextSong = songStore((state:any) => state.nextSong);
//  const prevSong = songStore((state:any) => state.prevSong); 



  return (
    <>
      <div>
    <Header/>
      </div>
    </>
  );
}

export default App;
